# ============================================
# PCMS 环境变量配置
# ============================================

# 数据库端口 (宿主机暴露端口)
DB_PORT=5432

# 后端服务端口 (宿主机暴露端口)
SERVER_PORT=8081

# 前端端口 (宿主机暴露端口)
WEB_PORT=80

# JWT 签名密钥 (生产环境务必修改)
JWT_SECRET=change-me-to-a-random-string
